// Dev placeholder. In `pnpm dev` this leaves apiBase empty so the app falls
// back to import.meta.env.VITE_API_BASE (from .env). In the container this file
// is replaced at startup with the value from the VITE_API_BASE env var.
window.__CONFIG__ = { apiBase: "" };
