# Hosts the charging stack runs on. Add future workloads as their own groups on
# the same boxes; the bootstrap + nginx roles are written to coexist.
[charging]
appmire-hetz1 ansible_host=136.243.103.58 ansible_user=ansible ansible_port=22

[charging:vars]
ansible_python_interpreter=/usr/bin/python3
ansible_ssh_private_key_file=~/.ssh/id_ed25519

# Current production box — the data-migration source (db dump). Auth via the
# ssh agent (matches ~/.ssh/config host appmire4-web).
[source]
appmire4-web ansible_host=65.21.150.133 ansible_user=jeroentrappers ansible_port=2202

[source:vars]
ansible_python_interpreter=/usr/bin/python3
charging_dir=/opt/charging
